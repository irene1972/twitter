# EN DEV
    1. Para que inicie el servidor en el backend, ir a la carpeta backend:
        npm run dev
    2. Para que inicie el frontend ejecutar ir a la carpeta frontend_:
        ng s
    2. Para verificar que llegan los emails se usa:
        https://mailtrap.io/inboxes

